// trim- string method 
//String.trim()

 const text ="             hii its me.           ";     // the wide space before stating the sentneces is called leading and the wide space after completeting if the senstence is called trailing 

 console.log(text.length);   // it will counts the spaces in the total length of the  text  
console.log(text); //

 console.log(text.trim);